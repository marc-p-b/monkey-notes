server {
    listen 80;
    server_name <<HOST>>;
    server_tokens off;
    client_max_body_size 500M;

    root /var/www/ui/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

