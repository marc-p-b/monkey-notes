server {
    listen 80;
    server_name localhost;
    server_tokens off;
    client_max_body_size 500M;

    root /var/www/mn-ui/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

}
